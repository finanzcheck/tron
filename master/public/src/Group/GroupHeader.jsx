import React, {Component} from 'react';
import Header from '../shared/Header/Header.jsx';

export default class GroupHeader extends Header {
}

GroupHeader.defaultProps = {
    classNames: ['panel-heading']
};
